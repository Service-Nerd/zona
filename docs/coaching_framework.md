A Coaching Framework — Plan Generation Rules (v4 Final)
 
1. User Inputs

All inputs must have sensible defaults and use progressive disclosure.
 
Core Inputs
Input	Type	Notes
Race date	Date	Required. Anchor for entire plan
Race distance	Select	5K / 10K / Half Marathon / Marathon / 50K / 100K
Goal	Select	Finish / Time target
Target time	Text	Only if goal = time target
Current weekly volume	Number (km)	Average of last 2–4 weeks
Longest recent run	Number (km)	Last 4–6 weeks
Days available per week	Number (2–6)	Hard constraint
Fitness level	Select	Beginner / Intermediate / Experienced
Zone 2 HR ceiling	Number	Default = 145 bpm
Resting HR	Number	Optional
Max HR	Number	Optional
Race name	Text	Optional
🧩 Lifestyle Constraints

Hard Constraints
Input	Type	Notes
Days cannot train	Multi-select	Must never be scheduled
Max weekday training time	Number (mins)	Caps session duration
Max weekend training time	Number (mins)	Caps long run duration
Travel / holiday dates	Date range	Blocks or reduces training
Soft Constraints
Input	Type	Notes
Preferred training days	Multi-select	Try to prioritise
Preferred long run day	Select	Default = Sunday
🧠 Behaviour & Psychology
Input	Type	Notes
Training style	Select	Predictable / Variety / Minimalist / Structured
Hard session relationship	Select	Avoid / Neutral / Love / Overdo
Motivation type	Select	Identity / Achievement / Health / Social
Why this race?	Text	Used for tone and engagement
🏃 Environment & Context
Input	Type	Notes
Terrain access	Select	Road / Trail / Mixed
Weather constraints	Select	Heat / Cold / None
Cross-training options	Select	Bike / Swim / Gym / None
🩺 Health & Injury
Input	Type	Notes
Injury history	Multi-select	Achilles / Knee / Back / Other
Current niggles	Text	Adjusts training load
Strength experience	Select	None / Some / Regular
2. Plan Philosophy
•	Life-first, plan-second
•	Plans must adapt to constraints, not the user
•	Consistency > optimisation
•	Adaptation > perfection
•	Minimum effective dose
•	Time-on-feet > distance (for beginners and ultra runners)
 
3. Plan Structure by Distance
 
5K / 10K (8–12 weeks)
•	Phase 1 — Base (Weeks 1–3): Zone 2 only
•	Phase 2 — Build (Weeks 4–8): Introduce 1 quality session/week
•	Phase 3 — Peak (Weeks 9–10): Highest volume, 1–2 quality sessions
•	Phase 4 — Taper (Weeks 11–12): Reduce volume 30–40%
 
Half Marathon (12–16 weeks)
•	Phase 1 — Base (Weeks 1–4)
•	Phase 2 — Build (Weeks 5–10)
•	Phase 3 — Peak (Weeks 11–13)
•	Phase 4 — Taper (Weeks 14–16)
 
Marathon (16–20 weeks)
•	Phase 1 — Base (Weeks 1–5)
•	Phase 2 — Build (Weeks 6–14)
•	Phase 3 — Peak (Weeks 15–17)
•	Phase 4 — Taper (Weeks 18–20)
 
50K (16–20 weeks)
•	Same as marathon
•	Back-to-back long runs in peak phase
•	Terrain-specific work where applicable
 
100K (20–24 weeks)
•	Phase 1 — Base (Weeks 1–6)
•	Phase 2 — Build (Weeks 7–16)
•	Phase 3 — Peak (Weeks 17–19)
•	Phase 4 — Taper (Weeks 20–24)

Focus:
•	Time on feet
•	Back-to-back long runs
•	Terrain specificity
 
4. Core Progression Rules
 
Volume Rules
•	Weekly volume increase ≤ 10%
•	Step-back every 3–4 weeks: reduce volume 20–25%
•	Long run ≤ 35% of weekly volume
•	No simultaneous increase in:
o	Volume + intensity
 
Intensity Distribution
•	Beginner: 90% easy / 10% hard
•	Intermediate+: 80% easy / 20% hard

Easy = Zone 1–2 / conversational effort
 
Time Cap Rules
•	Weekday sessions ≤ user max or 90 mins default
•	Long run caps:
o	HM: ≤ 2 hours
o	Marathon: ≤ 3 hours
o	50K: ≤ 4 hours
o	100K: ≤ 5–6 hours
 
Dual Mode Training

Plans operate in:
•	Distance-based mode
or
•	Time-based mode

Trigger time-based if:
•	Beginner
•	Limited time availability
•	Ultra training focus
 
5. Long Run Progression
•	Increase by:
o	2–3 km/week OR
o	5–10 mins/week (time-based)
•	Max increase: 20% per week
•	Step-back: reduce 15–20%
•	Peak targets:
Distance	Peak Long Run
5K	8–10 km
10K	12–15 km
HM	20–22 km
Marathon	32–35 km
50K	35–40 km
100K	45–55 km
6. Weekly Structure Engine
 
Default Structure
Day	Type
Mon	Rest / Strength
Tue	Easy
Wed	Quality
Thu	Rest / Easy
Fri	Easy
Sat	Optional / Easy
Sun	Long run
Placement Rules
•	Long run = preferred day
•	Quality session = midweek
•	Easy runs fill remaining days
 
Hard Constraints
•	Never schedule on unavailable days
•	Respect max session durations
•	Respect travel/holiday blocks
 
Spacing Rules
•	Minimum 48h around quality sessions
•	No quality session:
o	Day before long run
o	Day after long run
•	No stacking high-fatigue days
 
7. Quality Session Rules
•	None for beginners in first 3–4 weeks
•	Standard: 1 per week
•	Max: 2 per week
•	Maintain frequency in taper, reduce volume
•	Adjust based on:
o	Psychology (avoid/overdo)
o	Injury history
 
8. Session Types by Phase
Phase	Zone 2	Quality	Long Run	Strength
Base	2–3	0	1	2
Build	2	1	1	2
Peak	1–2	1–2	1	1–2
Taper	1	1	1 (short)	1
9. Quality Session Types by Distance

5K / 10K
•	Tempo: 20–40 min
•	Intervals: 400m–800m repeats

Half Marathon
•	Tempo: 30–50 min
•	Cruise intervals: 2–3 km reps

Marathon
•	Marathon pace runs
•	Threshold runs

50K / 100K
•	Race-pace long runs
•	Back-to-back long runs
•	Hill reps
 
10. Strength & Cross Training
•	Base/Build: 2 sessions/week
•	Peak: 1–2 sessions
•	Taper: 1 session

Rules:
•	Do not precede quality sessions
•	Adjust based on experience level
 
11. Adaptive Coaching Rules
 
Missed Sessions
Scenario	Action
Miss 1	Continue
Miss long run	Reschedule or freeze progression
Miss 3+	Reduce next week by 20%
Miss full week	Repeat previous week
Miss during taper	Do not compensate
Fatigue Adjustment

Triggers:
•	Elevated HR vs effort
•	High perceived effort
•	Poor sleep / high stress

Actions:
•	Reduce next week volume 10–20%
•	Remove or downgrade quality session
 
Consistency Rule
•	Track % completion weekly
•	Low consistency → slow progression
•	High consistency → allow progression
 
12. Injury-Aware Adjustments
Injury	Adjustment
Achilles	Reduce speed work and hills
Knee	Reduce volume spikes
Back	Limit long run duration
General rule:
•	Persistent pain (>3 runs) → reduce load or rest
 
13. Environment Adjustments
•	Heat: reduce intensity, increase recovery
•	Cold: extend warm-up
•	Trail: reduce pace expectations, increase effort focus
 
14. Race Preparation Rules
•	Practice fueling during long runs
•	Test race kit
•	Include race simulation sessions
•	Rehearse pacing and timing
 
15. Taper Rules
•	Duration:
o	5K/10K: 1 week
o	HM: 2 weeks
o	Marathon+: 2–3 weeks
•	Reduce volume 30–40% per week
•	Maintain intensity
•	No new variables (shoes, food, routine)
•	Prioritise:
o	Sleep
o	Nutrition
 
16. Guard Rails
Scenario	Action
Race < 6 weeks (marathon+)	Refuse
Race < 3 weeks	Refuse
Volume = 0 & race < 8 weeks	Warn
Days available < 2	Refuse
Unrealistic target	Flag gap
Long run exceeds safe %	Adjust
Over-constrained schedule	Simplify plan
Weekday time < 45 min	Simplify sessions
17. Confidence Scoring

Score (1–10) based on:
•	Time to race
•	Fitness vs goal gap
•	Lifestyle constraints
•	Consistency likelihood
•	Injury risk

Output:
•	Score
•	Risks
•	Suggested adjustments
 
18. Output Requirements

Each plan must:
•	Start on a Monday
•	Have continuous weekly structure
•	Include all 7 days
•	Respect all constraints

Each session must include:
•	Type
•	Description
•	Duration or distance
•	Effort (HR / pace / RPE)
 
19. Plan Integrity Rules
•	Never violate hard constraints
•	Never exceed progression limits
•	Always prioritise sustainability over optimisation
•	If conflict occurs:
→ simplify, don’t force complexity


Follow on converation:
Let me read it properly first.


Viewed a file, ran a command
Viewed a file, ran a command
Good document. Seriously solid foundation — you've thought this through properly. Here's my honest read.

What's strong and ready to build from:

Sections 1–9, 15–16, 18–19 are genuinely well-defined. The guard rails are tight, the progression rules are clear, the phase structures make sense. This is the kind of brief that means the generator will produce coherent plans rather than plausible-looking nonsense.

The lifestyle constraints section (hard/soft/psychology) is particularly good and goes beyond what I drafted — the training style and motivation type inputs are smart because they'll shape the tone of session descriptions, not just the structure.

What needs resolving before we build:

1. Adaptive coaching (Section 11) — how does this actually work? The rules are there but the mechanism isn't. "Missed 3+ sessions → reduce next week by 20%" requires something watching completion data and reshaping the plan. Is this:

R20 (Dynamic plan reshaping) territory — i.e. not part of the generator itself?
Or does the generator produce a static plan, and adaptive logic is a separate layer?
Clarify this or we'll accidentally build half of R20 into R23.

2. Confidence scoring (Section 17) — this is R18 It's listed here as part of the generator output but it's already a separate backlog item. Decision needed: does the generator produce a confidence score at creation time, or does R18 stay separate and score ongoing plan health? Both are valid but they're different things.

3. The output schema isn't specific enough Section 18 says each session needs type, description, duration/distance, and effort. But your actual JSON schema has more fields (label, detail, zone, hr_target, key). The generator needs to produce valid ZONA JSON — do we want it to output the full gist-compatible format, or something we then transform? This is a build decision we need to make before we write the prompt.

4. Time-based vs distance-based mode (Section 4) You mention plans operating in either mode — but your current JSON schema uses distance in km. Do we need to add a duration field as the primary field for time-based plans? If yes, the schema needs updating before generation.

5. Strength session content Section 10 says when to schedule strength but not what the sessions contain. Do generated strength sessions just say "strength training — 45 mins" or do they include specific exercises? Scope decision.

Three questions I need answers to before we start:

Is the plan generator purely a creation tool (outputs a static plan), or does it also include reshaping/adaptation? Because if it's the latter, we're building R20 and R23 simultaneously.
Does the generator output directly to a Supabase table, or does it output JSON that the user then saves as a gist? This affects the entire architecture.
Free or paid? (Pretty sure we said paid, just want it confirmed before we tag it.)
Answer those three and we're ready to build.

Yes we need to keep Dynamic re-shaping separate. Lets separate these out. 1. Plan creation: We need all data points and considerations. 2. Dynamic plan re-shaping based on rules, criteria and that can be based on rules and AI.
What do you think? I think keep separate but design so we can plug in
We need relevant keys in teh JSON. So make the determination based on what you know.
Yes we should be dynamic in what we can use duration or distance, thatn can be specific to plan or session. So perhaps a toggle on each session to allow people to select what they want to do, but we should suggest based on where they are as a runner when plan is created
lets keep strength as a separate backlog item. I think we already have it.
Questions to answer:

Creation at the minute as mentioned above in point 1.
I think we output as JSON.
Free should be very basic select a pre-loaded plan, Paid is dynamic based on the athlete variables Anything we do we need to consider the architecture and strategy going forward so we dont lock ourselves in. e.g. output jSON and then transform to supabase as opposed to straight to supabase. That allows us to change databases later down the line if needed. Any key decisions we make i want you to update memory so we keep sight of that as we move forward. Anything we decide or design we need to consider the user first and foremost and ensure it follows our brand guidelines and adheres to our brand.
Show more
09:56
Right. Let me lock this down properly before we write a single line.

Architecture decisions — recording these so we don't forget them in three months:

Plan output: JSON first, always. Generator produces a validated ZONA plan JSON object. That gets saved wherever — gist for now, Supabase table later, S3 after that, whatever. The app doesn't care where it came from as long as it's valid JSON matching the schema. No direct-to-database writes from the generator.

Creation vs reshaping: Completely separate concerns. Generator creates a static plan. Reshaper (R20) takes an existing plan + context and modifies it. They share the same schema and the same rule set — but different inputs, different outputs, different UI. Design the JSON schema so both can use it without modification.

Free vs paid: Free = pick from a small library of pre-built template plans (5K beginner, 10K intermediate, HM, Marathon). No personalisation, no AI. Paid = full generator with athlete variables, AI-generated session descriptions, confidence scoring. Clean line, easy to enforce.

Duration vs distance: Both are valid session targets. Store both in the schema where relevant. Suggest distance for experienced runners, time for beginners and ultra training. User can toggle per session. Schema needs both fields optional, with a primary_metric flag indicating which one to surface.

Strength sessions: Already in backlog as R21. Generator acknowledges them in the schedule (day placement, frequency) but session content is a stub — "Strength session — 45 min" — until R21 fills it in.


Viewed memory edits
Viewed memory edits
Good. Now let me update memory with all the key architectural decisions we've just made.


Used 2 tools
Used 2 tools
Good. Now let me lay out clearly where we are and what needs nailing before R23 starts.

What's locked:

The framework doc is genuinely solid. You've got the inputs, the rules, the guard rails, and the phase structures. That's the hard part — and you've done it offline which means we're not going to be making coaching decisions mid-build.

The architecture is clean:

JSON out, always
Creation and reshaping are separate siblings, not nested features
Free vs paid line is clear
Schema handles both duration and distance without forcing a choice
What still needs resolving before we write code:

The JSON schema itself. This is the most important thing we haven't done yet. We need to define the exact output format the generator produces — every field, every optional field, the primary_metric flag, how phases are labelled, how the confidence score is attached. This schema is the contract between R23 and R20 and R18. Get it wrong now and we'll be untangling it for months.

The generator prompt. This is the actual Claude API call that turns athlete variables into a plan. It needs to be tight enough that the output is always valid JSON matching the schema, always follows the coaching rules, and produces session descriptions that sound like ZONA — not a generic fitness app. Writing a good prompt for this is probably 2-3 hours of work on its own.

The user flow. Where does the generator live in the app? Is it a new screen, a modal, a separate page? What happens after the plan is generated — do they see a preview before it's saved? Can they regenerate? What does the ZONA brand voice sound like in this context?

My suggestion for what we do next:

Before any code — I draft the JSON schema and the generator prompt, you review both and push back. Then we mockup the UI. Then we build.

Want to start with the schema, or do you want to sort the session card redesign first since that's already been flagged?

i think we store all this, decisions made and come back to it. I want to look at the session card re-design. Lets go for that. What do you need from me to make this perfect?